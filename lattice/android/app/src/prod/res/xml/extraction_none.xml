<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (dataExtractionRules): nothing leaves the device, for cloud
     backup and for device-to-device transfer alike. A missing or empty
     section means "everything", so every domain is excluded at its root in
     both. The data-only counterpart is extraction_data.xml; srt pack swaps
     this file's bytes for it when the app opts in. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
