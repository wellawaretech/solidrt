name: Linux system dependencies
description: >
  apt-get install the SDL/build dependencies needed to build the host Linux
  binaries, with retries so a transient mirror hiccup does not fail the job.

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        for i in 1 2 3; do
          sudo apt-get update && sudo apt-get install -y \
            libclang-dev clang \
            build-essential git make pkg-config cmake ninja-build gnome-desktop-testing \
            libwayland-dev libdecor-0-dev libpipewire-0.3-dev liburing-dev \
            libx11-dev libxext-dev libxrandr-dev libxcursor-dev libxfixes-dev libxi-dev libxss-dev libxtst-dev \
            libxkbcommon-dev libdrm-dev libgbm-dev \
            libgl1-mesa-dev libgles2-mesa-dev libegl1-mesa-dev \
            libasound2-dev libpulse-dev libaudio-dev libfribidi-dev libjack-dev libsndio-dev \
            libdbus-1-dev libibus-1.0-dev libudev-dev libthai-dev libusb-1.0-0-dev \
            && exit 0
          echo "apt-get failed (attempt $i), retrying in 15s"; sleep 15
        done
        exit 1