# @solidrt/linux-arm64-gnu

SolidRT native binary for Linux arm64 (glibc), e.g. Raspberry Pi 4/5 on a 64-bit OS. This is an optional dependency of `@solidrt/cli` and is installed automatically - you do not need to install it directly.
